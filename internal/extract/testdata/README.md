# Test pages

These pages are synthetic. They copy the structure the platforms are known to use (JSON-LD, Next.js `__NEXT_DATA__`, the Meetup Apollo cache, iCal), but every person in them is invented. Replace or extend them with real pages saved by `speakertrail fetch --save`, and invent the names there too before committing.
